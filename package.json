{
  "name": "clean-match",
  "version": "1.0.0",
  "private": true,
  "description": "Clean Match — cleans ledger files and fuzzy-matches accounts side by side, preparing them for the Trial Balance vs Internal reconciliation tool",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node test/ledger-test.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "express-rate-limit": "^8.5.2"
  }
}
